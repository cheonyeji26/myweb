// 변수 선언
let message = "안녕하세요! 자바스크립트입니다.";
const count = 10;

// 콘솔에 출력
console.log(message);
console.log("숫자:", count);

// 웹 페이지에 직접 출력 (HTML 파일 내 <script> 태그 안에서 실행하거나, HTML 요소에 접근해야 함)
// document.body.innerHTML = `<h1>${message}</h1><p>숫자: ${count}</p>`;